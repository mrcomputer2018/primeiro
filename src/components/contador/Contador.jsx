import React, { Component } from "react"

class Contador extends Component {
    render() {
        return(
            <div>
                <h2>Contador</h2>
            </div>
        )
    }
}

export default Contador;